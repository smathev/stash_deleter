"""
Unit tests for plugin/config_loader.py.

Verifies that YAML config is loaded, defaults applied, and invalid config rejected.
"""


class TestConfigLoader:
    """Tests for ConfigLoader — YAML loading and validation."""
